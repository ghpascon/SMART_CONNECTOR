# Warning: using --force may impact tag history, evaluate if it is really necessary
# Optimization: adds only the project folder, avoiding git add -A which can be slow in large repositories
# Optimization: adds only the project folder, avoiding git add -A which can be slow in large repositories
name: Test Build on Windows and Linux

on:
  workflow_dispatch:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Poetry and dependencies
        run: pip install poetry; poetry lock; poetry install

      - name: Run tests
        run: poetry run pytest

      - name: Build executable
        run: poetry run python scripts/build_exe.py

      - name: Checkout executables repo
        uses: actions/checkout@v4
        with:
          repository: ghpascon/executables
          token: ${{ secrets.EXECUTABLES_TOKEN }}
          path: executables-repo
          ref: main
          fetch-depth: 1

      - name: Remove old project folder and copy executables
        shell: pwsh
        run: |
          $projectName = $env:GITHUB_REPOSITORY.Split('/')[1]
          $destDir = "executables-repo/windows/$projectName"

          # Remove only the old project folder
          Remove-Item -Path $destDir -Recurse -Force -ErrorAction SilentlyContinue
          New-Item -ItemType Directory -Force -Path $destDir

          Copy-Item -Path TEMP\* -Destination $destDir\ -Recurse -Force

      - name: Commit and push executables
        shell: pwsh
        run: |
          cd executables-repo
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add windows/$projectName
          git commit -m "Update Windows executables for $projectName" || echo "No changes to commit"
          git push
  build-linux:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Poetry and dependencies
        run: |
          pip install poetry
          poetry lock
          poetry install

      - name: Run tests
        run: poetry run pytest

      - name: Build executable
        run: poetry run python scripts/build_exe.py

      - name: Upload Linux build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: linux-build
          path: TEMP

  publish-executables:
    needs: [build-windows, build-linux]
    runs-on: ubuntu-latest
    steps:
      - name: Download Linux build artifact
        uses: actions/download-artifact@v4
        with:
          name: linux-build
          path: build_dir

      - name: Checkout executables repo
        uses: actions/checkout@v4
        with:
          repository: ghpascon/executables
          token: ${{ secrets.EXECUTABLES_TOKEN }}
          path: executables-repo
          ref: main
          fetch-depth: 1

      - name: Remove old project folder and copy executables
        run: |
          project_name=$(basename $GITHUB_REPOSITORY)
          build_dir="build_dir"
          dest_dir="executables-repo/linux/$project_name"

          # Remove only the old project folder
          rm -rf "$dest_dir"
          mkdir -p "$dest_dir"

          if [ -d "$build_dir/TEMP" ]; then
            rsync -av --exclude=build/ "$build_dir/TEMP/" "$dest_dir/"
          elif [ -d "$build_dir" ]; then
            rsync -av --exclude=build/ "$build_dir/" "$dest_dir/"
          else
            echo "Directory $build_dir not found, build may have failed."
            exit 1
          fi

      - name: Commit and push executables
        run: |
          project_name=$(basename $GITHUB_REPOSITORY)
          cd executables-repo
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add linux/$project_name
          git commit -m "Update Linux executables for $project_name" || echo "No changes to commit"
          git push

      - name: Determine tag to use
        run: |
          project_name=$(basename $GITHUB_REPOSITORY)
          cd executables-repo
          git fetch --tags

          origin_tag=$(git ls-remote --tags https://github.com/$GITHUB_REPOSITORY.git | awk -F/ '{print $3}' | sort -V | tail -n1)

          if [ -n "$origin_tag" ]; then
            NEW_TAG="${project_name}-${origin_tag}"
            echo "Using tag from origin repo: $NEW_TAG"
          else
            last_tag=$(git tag --list "${project_name}-v*" | sort -V | tail -n1)
            if [ -z "$last_tag" ]; then
              NEW_TAG="${project_name}-v1.0.0"
            else
              version="${last_tag##*-v}"
              major=$(echo $version | cut -d. -f1)
              minor=$(echo $version | cut -d. -f2)
              patch=$(echo $version | cut -d. -f3)
              patch=$((patch + 1))
              NEW_TAG="${project_name}-v${major}.${minor}.${patch}"
            fi
            echo "Generated incremental tag: $NEW_TAG"
          fi

          echo "NEW_TAG=$NEW_TAG" >> $GITHUB_ENV

      - name: Create and push tag
        run: |
          cd executables-repo
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git tag -f $NEW_TAG
          git push origin $NEW_TAG --force
